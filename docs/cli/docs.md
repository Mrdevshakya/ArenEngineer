---
summary: "CLI reference for `aren docs` (search the live docs index)"
read_when:
  - You want to search the live Aren docs from the terminal
title: "docs"
---

# `aren docs`

Search the live docs index.

```bash
aren docs browser extension
aren docs sandbox allowHostControl
```
