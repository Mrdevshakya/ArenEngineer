---
summary: "CLI reference for `aren uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `aren uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
aren uninstall
aren uninstall --all --yes
aren uninstall --dry-run
```
