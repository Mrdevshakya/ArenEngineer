---
summary: "CLI reference for `aren sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `aren sessions`

List stored conversation sessions.

```bash
aren sessions
aren sessions --active 120
aren sessions --json
```
